//Solution in JAVA
//Given two positive integers A and B, find GCD of A and B.

class Solution
{
    public int gcd(int A , int B) 
    { 
        //code here
        if(B==0){
            return A;
        }
        return gcd(B,A%B);
    } 
}
